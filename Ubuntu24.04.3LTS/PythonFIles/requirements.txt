# Ubuntu 24.04 / Raspberry Pi — Python 3.12 disarankan (sesuai repo Ubuntu)
# Buat venv: python3 -m venv .venv && source .venv/bin/activate
# Pasang dependensi sistem (contoh): sudo apt install python3-pip python3-venv libgl1 libglib2.0-0

opencv-python>=4.8.0
numpy>=1.24.0
PySide6>=6.6.0
mediapipe>=0.10.0
ultralytics>=8.0.0
