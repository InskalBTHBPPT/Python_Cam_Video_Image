# Dependensi umum (Level 1–6, GUI). Python 3.10+.
#
# Raspberry Pi 4 (aarch64) + Level 7 / YOLO:
#   JANGAN hanya file ini — pakai requirements-raspberrypi.txt atau skrip fix_pytorch_raspi.sh
#   (lihat user-manual.md §4.2).
#
# PC Ubuntu / x86_64 (tanpa masalah Illegal instruction):
#   pip install -r requirements.txt

opencv-python>=4.8.0
numpy>=1.24.0
PySide6>=6.6.0
mediapipe>=0.10.0
ultralytics>=8.0.0
